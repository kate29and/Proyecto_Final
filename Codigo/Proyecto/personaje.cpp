#include "personaje.h"


/*Personaje::Personaje(int x, int y, int width, int height)
    : QGraphicsRectItem(x, y, width, height)
{
    posx = 0;
    posy = 0;
    velocidad = 0;
    color = Qt::lightGray;
}

int Personaje::getPosx() const {
    return posx;
}

void Personaje::setPosx(int newPosx) {
    posx = newPosx;
}

int Personaje::getPosy() const {
    return posy;
}

void Personaje::setPosy(int newPosy) {
    posy = newPosy;
}

int Personaje::getVelocidad() const {
    return velocidad;
}

QColor Personaje::getColor() const {
    return color;
}

void Personaje::setColor(const QColor& newColor) {
    color = newColor;
}

QRectF Personaje::boundingRect() const {
    return QRectF(-10, -10, 20, 20);  // Tamaño y posición del rectángulo de colisión del personaje
}

void Personaje::paint(QPainter* painter, const QStyleOptionGraphicsItem* option, QWidget* widget) {
    painter->setBrush(color);
    painter->drawEllipse(-10, -10, 20, 20);  // Dibuja un círculo para representar al personaje
}*/
